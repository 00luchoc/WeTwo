# WeTwo

## TESTS BACK/FRONT

### Back

Para ejecutar los tests de backend primero se debe clonar el repo y acceder de la siguiente manera:

`cd WeTwo-Back/test`

Una vez dentro de la carpeta test, abrir la terminal y ejecutar el siguiente comando para ejecutarlos:

`pytest -v`
